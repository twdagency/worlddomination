import { describe, it } from 'vitest';

/**
 * Sprint 10 phase contracts — implemented in Phases 1–8.
 * Phase 0 placeholders only; no production code changes.
 */
describe('Sprint 10 contracts', () => {
  it.todo(
    'Phase 1: country.ts does not import influenceActions or influenceAccelerators directly',
  );

  it.todo(
    'Phase 3: Faction type alias remains valid for backward compat after Country canonicalization',
  );

  it.todo(
    'Phase 4: AI accelerator scoring applies leader posture modifier (e.g. opportunist favors Subversion)',
  );

  it.todo(
    'Phase 4: AI issues Diplomatic Mission against player-owned city when influence gap and posture favor it',
  );

  it.todo(
    'Phase 5: AI issues Coup against player city when threshold, gold, and posture favor high-risk transfer',
  );

  it.todo(
    'Phase 6: Intelligence action at 30+ influence emits intelReport with enriched snapshot fields',
  );

  it.todo(
    'Phase 7: Annexation at 70+ influence transfers ownership peacefully and applies reputation cascade',
  );

  it.todo(
    'Phase 8: tutorial playthrough beat sequence unchanged — AI influence orders suppressed in tutorial scenario',
  );
});
