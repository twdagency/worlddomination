{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // No rootDir/outDir: this project is type-check only, and rootDir would
    // reject the cross-package imports from `packages/shared`.
    "noEmit": true
  },
  "include": ["src/**/*", "tests/**/*", "perf/**/*"]
}
